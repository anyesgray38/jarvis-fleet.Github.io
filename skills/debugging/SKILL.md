# Debugging Skill

REPRODUCE → ISOLATE → HYPOTHESIZE → CHANGE → TEST → VERIFY.

Capture exact errors and environment state. Change one meaningful variable at a time when possible. Do not declare a fix until the original failure is reproduced as resolved.
